name: "hello-world"
principal: "test-principal"
api-port: {{PORT0}}
pods:
  pod-type:
    container:
      image-name: ubuntu:14.04
      volumes:
        - mode: RW
          container-path: /data
          host-path: persistent-volume/data
          size: 50
          type: ROOT
    count: 1
    tasks:
      test-task:
        goal: RUNNING
        cmd: "./task-cmd"
        cpus: 0.1
